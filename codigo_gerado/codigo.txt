#define max 100
{
	scanf( altura, largura);
	area = (altura * largura );
	if( (quantidade + 1 )>= >= 30 )
	{
		if( cont = == 0 )
		{
			for(cont = 0 ; cont < max ; cont++)
			{
				quantidade = area * cont ;
				printf(quantidade);
				cont = cont + 1 ;
			}
;
		}
		else
		{
			printf(quantidade);
		}
;
	}
;
}
